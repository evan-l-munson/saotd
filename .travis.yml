# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: r
#r:
#  - oldrel
#  - release
#  - devel 

#sudo: required
cache: packages

install:
  - R -e 'install.packages(c("ggraph", "ldatuning", "topicmodels"))'
  
#before_install:
#  - sudo apt-get install libgmp3-dev
#  
r_packages:
  - covr

after_success:
- Rscript -e 'library(covr); codecov()'